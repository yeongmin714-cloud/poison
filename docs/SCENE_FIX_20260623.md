# 🔧 MainScene 통합 문제 수정 내역 (2026-06-23)

## 수정 완료 항목 (자동 처리)

### 1. 🏔️ Poly Haven 모델 → 추후 단순화 가능 (Editor 스크립트 생성)
- **파일:** `Assets/Editor/PolyHavenSimplifier.cs`
- **백업:** `Assets/Scenes/MainScene_PolyHaven_Backup.unity` (12MB) ✅
- **백업:** `Assets/Models/PolyHeven_Backup.tar.gz` (458MB) ✅
- **사용법:** Unity 열고 `Tools > Poly Haven > Replace With Primitives`
- **복원:** `Tools > Poly Haven > Restore From Backup`
- PolyHeven 폴더는 그대로 유지 (원본 GLTF 모두 있음)

### 2. 🦎 MonsterSpawner 영역 버그 수정
- `advancedOuter: 1000` → `advancedOuter: 1800`
- Advanced 티어 몬스터가 1200~1800m 링에 정상 스폰됨

### 3. 📷 카메라 이중 구조 해결
- **Player Camera** (`CinemachineCamera + ThirdPersonFollow`) → 비활성화
- **Main Camera의 CinemachineBrain** → 완전 제거
- **결과:** `TopDownCameraController`가 유일한 카메라 컨트롤러로 동작

### 4. 🐍 SnakeSlitherMotion 제거
- Player에 붙어있던 잘못된 Snake 애니메이션 모션 제거
- (Player_Rigged.glb는 인간형 — 뱀 모션은 부적합)

---

## Unity Editor에서 실행 필요 항목

### 5. Player 애니메이션 시스템 복구
- **실행:** `Tools > Scene Fix > Fix All Issues`
  - Player에 Animator + RigAnimationController 추가
  - PlayerPlaceholder 컴포넌트 추가
  - 잘못된 AnimationRiggingSetup 제거
  - MotionDetector 초기화

### 6. Poly Haven 단순화 (성능 개선) — 선택 실행
- **실행:** `Tools > Poly Haven > Replace With Primitives`
  - 나무 125그루 → 녹색 원기둥
  - 바위 90개 → 회색 박스
  - 식물 520개 → 초록 구체
  - **예상 효과:** 씬 오브젝트 890개 → 735개 Primitive로 교체, MeshCollider 제거로 물리 연산 90% 감소

### 7. (선택) Player 자식 본 정리
- **실행:** `Tools > Scene Fix > Remove PolyHaven Hierarchy Under Player`
  - Player 아래 90개의 GLB 스켈레톤 본 제거
  - PlayerPlaceholder가 런타임에 GLB 로드하면 자동 교체됨

---

## 복원 방법

### Poly Haven 모델 복원
- `Tools > Poly Haven > Restore From Backup`

### 전체 씬 복원
- `MainScene_PolyHaven_Backup.unity` → `MainScene.unity`로 복사

### PolyHeven GLTF 복원
- `Assets/Models/PolyHeven_Backup.tar.gz` 압축 해제
