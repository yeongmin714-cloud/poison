namespace ProjectName.Systems
{
    /// <summary>M2: 핫바(또는 UI)와 드라이버 간 모드 상태 브리지 — asmdef 경계 우회용.</summary>
    public static class PlayerWeaponModeBridge
    {
        public static bool ThrowSelected;   // 폭탄 슬롯 선택 중(투척 모드)
    }
}
