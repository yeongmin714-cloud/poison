namespace ProjectName.Core
{
    /// <summary>
    /// 컴파일 테스트용 헬퍼 클래스.
    /// 실제 동작은 하지 않으며, 컴파일러가 이 파일을 정상적으로 컴파일하는지 확인하는 용도로 사용됩니다.
    /// </summary>
    public static class CompileTestHelper
    {
        /// <summary>
        /// 아무 동작도 수행하지 않습니다.
        /// 이 메서드의 존재 여부로 컴파일 성공 여부를 테스트합니다.
        /// </summary>
        public static void DoNothing()
        {
            // 의도적으로 비워둠
        }
    }
}
